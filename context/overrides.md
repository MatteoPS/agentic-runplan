# Overrides log

Every explicit `OVERRIDE: <reason>` is appended here (§6 E4). Never assumed —
only a literal typed override adds an entry. If overrides exceed 2 in any
4-week block, `out/today.md` must say the plan isn't matching real life and
propose a structural revision, not more overrides (§6 E5).
