# TODO — future feature ideas

Not scoped or scheduled, just captured so they aren't lost. None of this
affects the current CLI/agent-driven workflow.

- **Edit pushed activities** only one HR slot (no warm up or cool down) when the HR target is constant
- **Edit pushed elliptical** test using "*elliptical*" as an activity, not a cardio workout.
- **PC GUI.** Some kind of desktop interface instead of (or alongside) the `mc` CLI + slash-command workflow.
- **Auto-send `out/` to phone.** Automatically get `out/today.md`/`.html` (and maybe `dashboard.html`) onto the phone each day, rather than opening them from the repo manually. -> Google Keep or Apple note?
- **Run on phone.** Get the system itself running on mobile, not just the
  rendered output.
- **Apple health integration** maybe?
