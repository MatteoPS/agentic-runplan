# TODO — future feature ideas

Not scoped or scheduled, just captured so they aren't lost. None of this
affects the current CLI/agent-driven workflow.

## Major 
- **Add `/plan` or `/tomorrow`** to know in advance the workout ahead. 
- **PC GUI** Some kind of desktop interface instead of (or alongside) the Claude slash-command workflow. 
- **Run on phone** (maybe follows the PC GUI or instead of) Get the system itself running on iphone, not just therendered output. Building a standalone iphone app that conencts to claude code (or claude API) from the phone. then it's crucial to sync the results with the laptop version. Maybe using Icloud or Google drive?

## Minor
- **Edit pushed activities** only one HR slot when the HR target is constant, no need of warmup and cooldown phase at the same HR target
- **Edit pushed elliptical** "*elliptical*" was pushed to garmin as a 'cardio workout', but garmin do have the elliptical as a workout. can it be pushed as 'elliptical' instead?
- **Auto-send `out/` to phone.** Automatically get `out/today.md`/`.html` (and maybe `dashboard.html`) onto the phone each day, rather than opening them from the repo manually. Google Keep or Apple Note?

## Maybe not
- **Apple health integration** mabye not worth it, just because I don't wear the garmin all the time but i pretty much always have my phone with me.
