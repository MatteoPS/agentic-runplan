# secrets
.env

# python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# generated / cache — regenerated by `mc sync`, contains real personal data
data/*
!data/README.md

# real personal training output/log — regenerated by `mc digest`/`mc render`/`/daily`
# see examples/ for fabricated sample output showing the real shape of these
/out/
/log/

# writer lease — belongs to the *state* repo, where it is committed so the
# other device can see it. Here (MC_STATE_DIR unset) it is local scratch.
/writer.json

# os
.DS_Store
